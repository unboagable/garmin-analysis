[tool.poetry]
name = "garmin_analysis"
version = "0.1.0"
description = "python projects, practice, and study."
authors = ["Chang-Hyun Mungai <changhyunmungai@gmail.com>"]
readme = "README.md"
packages = [{ include = "src/*.py" }]

[tool.poetry.dependencies]
python = "^3.12"
pandas = "*"
notebook = "^7.4.3"
ipykernel = "^6.29.5"
matplotlib = "^3.10.3"
scikit-learn = "^1.4.2"
seaborn = "*"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

